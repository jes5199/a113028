# a113028

A113028 — base-B digit-arrangement divisibility search. Two-engine solver (Engine S scan / Engine D prefix-DFS with tail brute), alternated with doubling budgets.

Build: `gcc -O2 -march=native -o a113028 a113028.c`
Run:   `./a113028 [lo] [hi] [verbose]`  e.g. `./a113028 2 48 1`

_Source pending: canonical a113028.c from jes (the Telegram paste dropped some operators)._
